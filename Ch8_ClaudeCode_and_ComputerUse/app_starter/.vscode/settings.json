{
  // 既定インタープリタはリポジトリ直下の .venv。章ごとの venv は各フォルダの pyproject [tool.pyright] か
  // 「Python: Select Interpreter」で切り替えてください（[フォルダ名] は言語 ID ではないため無効でした）。
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "jupyter.kernels.filter": []
}